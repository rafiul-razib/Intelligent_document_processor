# --- Web ---
Flask==3.1.3
Werkzeug==3.1.8
gunicorn==26.0.0

# --- Config ---
python-dotenv==1.2.2

# --- PDF & images ---
PyMuPDF==1.27.2.3
pdf2image==1.17.0
Pillow==12.2.0

# --- OCR & CV (EasyOCR + its stack) ---
# NumPy 2.2.x has no cp314 wheels on Windows; pip falls back to Meson and often fails.
# Use 2.4.x for Python 3.11–3.14. SciPy 1.17+ ships cp314 wheels (1.14.x does not).
easyocr==1.7.2
opencv-python-headless==4.13.0.92
numpy==2.4.4
scipy==1.17.1
scikit-image==0.26.0
python-bidi==0.4.2
PyYAML==6.0.3
Shapely==2.1.2
pyclipper==1.4.0
ninja==1.13.0
torch==2.11.0
torchvision==0.26.0

# --- Text utilities ---
langdetect==1.0.9
pypinyin==0.55.0

# --- Gemini (google.genai SDK) ---
google-genai==1.75.0
